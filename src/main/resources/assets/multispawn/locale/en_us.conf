multispawn {
  command {
    main {
      description: "&aReturn the closest spawn"
    }
    list {
      hover: "&aClick to {0}"
      description: "&aList all spawn"
      message: "&aSpawns：{0}"
      empty:"&cEmpty list!"
    }
    set {
      success: "&aSet {0} success"
      description: "&aSet spawn"
    }
    remove {
      success: "&aRemove {0} success"
      description: "&aRemove spawn"
    }
  }
  message {
    waitingForType: "&aPlease input spawn name"
  }
  nucleus {
    warp {
      category: "Spawns"
    }
  }
}